# Dev: Vite proxy maps /api → http://127.0.0.1:8080 (see vite.config.ts).
# You usually do NOT need VITE_API_URL when using `npm run dev`.
# VITE_API_URL=http://localhost:8080/api

# OAuth, OpenWeather, Gemini, Amadeus, etc. belong in backend/.env only — see ../ENV_SECRETS.md
